.DS_Store
node_modules/
*.log

# 웹(Workers) 빌드 산출물 — Vite 가 dist/web 에 SPA 생성.
dist/

# Cloudflare/Wrangler 로컬 상태(D1 시뮬·캐시) 및 wrangler types 생성본
.wrangler/
worker-configuration.d.ts

# 로컬 Workers secrets (wrangler dev 가 읽는 env) — 실제 자격은 커밋 금지
.dev.vars

# sqlite db (진실 데이터 — 코드 아님)
*.db
*.db-*
*.db.bak-*

# pi-subagents runtime artifacts
.pi-subagents/
